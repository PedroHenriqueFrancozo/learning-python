# Aula 04 - Tipos Numéricos (Numeric Types) 📋

Nesta aula aprendi sobre os dois principais tipos de números no Python: `int` e `float`.
In this class, I learned about the two main types of numbers in Python: `int` and `float`.

### :clipboard: O que aprendi / What I learned:

* **int (Integer):** Números inteiros, sem casas decimais. Ex: `10`, `-5`, `0`.
  *(Whole numbers, without decimal places).*
* **float (Floating Point):** Números decimais. **Importante:** Em Python, usamos o ponto `.` como separador, não a vírgula. Ex: `1.5`, `-10.11`.
  *(Decimal numbers. Important: In Python, we use a dot `.` as a separator, not a comma).*
* **type():** Uma função interna que retorna o tipo de um valor ou variável.
  *(A built-in function that returns the type of a value or variable).*

---

### 💡 Insight:
O Python é uma linguagem de **Tipagem Dinâmica**, o que significa que ele infere (adivinha) o tipo do dado automaticamente assim que você o escreve.
*(Python is a Dynamically Typed language, meaning it infers the data type automatically as soon as you write it).*