# Aula 09 - Operadores Aritméticos (Arithmetic Operators) 📋

Nesta aula aprendi como realizar cálculos matemáticos, desde os básicos até os mais específicos do Python.
In this class, I learned how to perform mathematical calculations, from basics to Python-specific ones.

### :clipboard: O que aprendi / What I learned:

* **Divisão (`/`):** Sempre retorna um número `float`.
* **Divisão Inteira (`//`):** Corta as casas decimais e retorna apenas o número inteiro.
* **Exponenciação (`**`):** Elevado à potência (ex: 2 elevado a 10).
* **Módulo (`%`):** Retorna o **resto** da divisão. É muito útil para:
    * Verificar se um número é par ou ímpar (`n % 2 == 0`).
    * Verificar se um número é múltiplo de outro.

---
### 💡 Insight:
A divisão inteira (`//`) é útil quando você precisa de um resultado exato em unidades, como calcular quantas páginas inteiras um livro terá baseado no total de linhas.