# Aula 10 - Concatenação e Repetição (Strings) 📋

Nesta aula aprendi que os operadores aritméticos também podem ser usados com textos.
In this class, I learned that arithmetic operators can also be used with text.

### :clipboard: O que aprendi / What I learned:

* **Concatenação (`+`):** Une dois textos. **Importante:** Não funciona para somar `str + int` (precisa de coerção).
* **Repetição (`*`):** Repete uma string um determinado número de vezes.

---
### 💡 Exemplo:
`'Luiz' + ' ' + 'Otávio'` resulta em `'Luiz Otávio'`.
`'A' * 5` resulta em `'AAAAA'`.