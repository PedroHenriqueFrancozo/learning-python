# Aula 19 - Operadores de Comparação (Relacionais) 📋

Nesta aula aprendi a utilizar os operadores relacionais, que permitem comparar valores e retornam sempre um valor Booleano (`True` ou `False`). Eles são fundamentais para a criação de condições em blocos `if`.
In this class, I learned how to use relational operators, which allow comparing values and always return a Boolean value (`True` or `False`).

### :clipboard: O que aprendi / What I learned:

* **Tipos de Comparação:** Como verificar se valores são maiores, menores, iguais ou diferentes.
* **Comparação de Strings:** O Python também permite comparar textos (ex: `'a' == 'a'`).
* **Retorno Booleano:** O resultado de qualquer comparação é sempre um dado do tipo `bool`.

---

### 🛠️ Tabela de Operadores / Operators Table:

| Operador | Significado | Exemplo | Resultado |
| :--- | :--- | :--- | :--- |
| `>` | Maior | `5 > 2` | `True` |
| `>=` | Maior ou Igual | `2 >= 2` | `True` |
| `<` | Menor | `1 < 8` | `True` |
| `<=` | Menor ou Igual | `10 <= 5` | `False` |
| `==` | Igual | `'abc' == 'abc'` | `True` |
| `!=` | Diferente | `'a' != 'b'` | `True` |

---

### 💡 Insight:
Um erro comum de iniciantes é confundir o sinal de atribuição `=` (usado para dar valor a uma variável) com o sinal de comparação `==` (usado para verificar se algo é igual a outro). Lembre-se: **Um sinal recebe, dois sinais comparam!**