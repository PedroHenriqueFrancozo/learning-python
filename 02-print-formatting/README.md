# Aula 02 - Formatando a Saída (Formatting Output) 📝

Nesta aula aprendi a controlar como os dados aparecem no console usando os argumentos `sep` e `end`.
In this class, I learned how to control how data appears in the console using the `sep` and `end` arguments.

### 📋 O que aprendi / What I learned:

* **sep (Separator):** Define o que aparece entre os valores. Por padrão é um espaço.
  *(Defines what appears between values. Defaults to a space).*
* **end:** Define o que aparece ao final da execução do print. Por padrão é uma quebra de linha `\n`.
  *(Defines what appears at the end of the print. Defaults to a newline `\n`).*
* **Line Breaks:**
    * `\n` (LF - Line Feed): Padrão Linux/Mac.
    * `\r\n` (CRLF - Carriage Return Line Feed): Padrão Windows.

### 💡 Insight:
Entender o `end` é fundamental para quando queremos que dois `print()` diferentes apareçam na mesma linha no terminal.
*(Understanding `end` is key when we want two different `print()` calls to appear on the same line in the terminal).*