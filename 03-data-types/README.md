# Aula 03 - Tipos de Dados e Strings (Data Types & Strings) 📋

Nesta aula, explorei como o Python lida com textos e sua característica de tipagem.
In this class, I explored how Python handles text and its typing characteristics.

### :clipboard: O que aprendi / What I learned:

* **Tipagem Dinâmica / Forte (Dynamic / Strong Typing):** O Python identifica o tipo de dado automaticamente, mas não permite operações inválidas entre tipos diferentes sem conversão.
* **Strings (str):** São textos delimitados por aspas simples (`'`) ou duplas (`"`).
* **Escape (`\`):** Usado para "pular" caracteres que o Python interpretaria de outra forma (ex: colocar aspas dentro de aspas).
* **Raw Strings (`r`):** Faz com que o Python ignore o caractere de escape e exiba tudo o que está dentro da string.

---

### 🛠️ Comandos Práticos / Practical Commands:

```python
print("Exemplo de \"Escape\"") # Exibe aspas internas
print(r"Exemplo com r\"")      # Exibe a barra invertida literalmente